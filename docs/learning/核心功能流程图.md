# OpenClaw 核心功能流程图

本文档包含 OpenClaw 项目的核心架构和业务流程图，帮助理解系统的整体设计和数据流。

## 目录

- [系统整体架构](#系统整体架构)
- [Gateway 启动流程](#gateway-启动流程)
- [消息处理生命周期](#消息处理生命周期)
- [Agent Session 处理流程](#agent-session-处理流程)
- [插件系统架构](#插件系统架构)
- [认证与授权流程](#认证与授权流程)
- [子代理协作流程](#子代理协作流程)
- [上下文引擎流程](#上下文引擎流程)
- [工具执行流程](#工具执行流程)

---

## 系统整体架构

```mermaid
flowchart TB
    subgraph "外部渠道层"
        A1[WhatsApp]
        A2[Telegram]
        A3[Slack]
        A4[Discord]
        A5[其他 20+ 渠道]
    end
    
    subgraph "客户端应用层"
        B1[macOS App]
        B2[iOS App]
        B3[Android App]
        B4[WebChat UI]
        B5[CLI Client]
    end
    
    subgraph "Gateway 控制平面 (Node.js)"
        direction TB
        
        C1[WebSocket RPC Server]
        C2[HTTP Server<br/>Hono/Express]
        C3[Plugin Runtime]
        
        subgraph "核心服务"
            D1[Session Manager]
            D2[Routing Engine]
            D3[Auth System]
            D4[Cron Scheduler]
        end
        
        subgraph "Agent 运行时"
            E1[Agent Session A]
            E2[Agent Session B]
            E3[Agent Session N]
        end
        
        subgraph "插件系统"
            F1[Channel Plugins]
            F2[Provider Plugins]
            F3[Feature Plugins]
        end
    end
    
    subgraph "AI 模型层"
        G1[OpenAI/Codex]
        G2[Anthropic]
        G3[Google Gemini]
        G4[Z.ai]
        G5[其他 Provider]
    end
    
    subgraph "工具与服务"
        H1[Browser Control<br/>Playwright]
        H2[System Commands<br/>Shell]
        H3[Memory System<br/>SQLite-vec]
        H4[MCP Servers]
        H5[Skills Registry]
    end
    
    A1 & A2 & A3 & A4 & A5 -->|Webhook/Polling| C1
    B1 & B2 & B3 & B4 & B5 -->|WebSocket| C1
    
    C1 <--> C2
    C1 <--> C3
    
    C3 --> D1 & D2 & D3 & D4
    C3 --> E1 & E2 & E3
    C3 --> F1 & F2 & F3
    
    E1 & E2 & E3 --> G1 & G2 & G3 & G4 & G5
    E1 & E2 & E3 --> H1 & H2 & H3 & H4 & H5
    
    style C1 fill:#f96,stroke:#333,stroke-width:2px
    style C2 fill:#f96,stroke:#333,stroke-width:2px
    style C3 fill:#f96,stroke:#333,stroke-width:2px
```

---

## Gateway 启动流程

```mermaid
flowchart TD
    Start([CLI 启动]) --> A[openclaw.mjs<br/>入口文件]
    A --> B[Node.js 版本检查<br/>>= 22.12]
    B --> C{版本符合？}
    
    C -->|否 | ExitErr([退出并提示升级])
    C -->|是 | D[环境变量标准化<br/>normalizeEnv]
    
    D --> E[启用编译缓存<br/>enableCompileCache]
    E --> F[安装进程警告过滤器]
    F --> G[解析 CLI Profile 参数]
    
    G --> H{容器化环境？}
    H -->|是 | I[解析容器目标]
    H -->|否 | J[构建 Respawn 计划]
    
    I --> K[容器内重启进程]
    J --> L{需要 Respawn?}
    L -->|是 | M[生成子进程<br/>spawn child]
    L -->|否 | N[启动 CLI 命令]
    
    M --> K
    K --> N
    
    N --> O{命令类型}
    O -->|gateway| P[启动 Gateway Server]
    O -->|message send| Q[发送消息]
    O -->|agent| R[运行 Agent]
    O -->|onboard| S[设置向导]
    O -->|其他 | T[执行对应命令]
    
    P --> U[HTTP Server 启动<br/>Control UI/WebChat]
    U --> V[WebSocket Server 启动]
    V --> W[加载插件系统]
    W --> X[注册 Channel Plugins]
    X --> Y[初始化 Agent Sessions]
    Y --> Z[启动 Cron Jobs]
    Z --> End([Gateway 就绪])
    
    style Start fill:#9f9,stroke:#333,stroke-width:2px
    style End fill:#9f9,stroke:#333,stroke-width:2px
    style ExitErr fill:#f99,stroke:#333,stroke-width:2px
```

---

## 消息处理生命周期

```mermaid
flowchart TD
    A[外部消息渠道<br/>WhatsApp/Telegram 等] --> B[Channel Plugin<br/>Webhook/Polling]
    B --> C{Gateway<br/>WebSocket Server}
    C --> D[消息解析<br/>Message Parser]
    D --> E{路由决策<br/>Routing Decision}
    
    E -->|私信 DM| F[配对验证<br/>Pairing Check]
    E -->|群组 Group| G[沙箱隔离<br/>Docker Sandbox]
    E -->|命令 Command| H[CLI Backend]
    
    F --> I{验证通过？}
    I -->|是 | J[创建 Agent Session]
    I -->|否 | K[返回配对码<br/>Pairing Code]
    
    G --> J
    H --> J
    
    J --> L[Context Engine<br/>加载历史]
    L --> M[构建 Prompt<br/>System + User]
    M --> N[LLM 调用<br/>Plugin SDK]
    
    N --> O{模型响应}
    O --> P[工具调用<br/>Tool Calls]
    O --> Q[文本回复<br/>Text Response]
    
    P --> R[执行工具<br/>Browser/Shell/MCP]
    R --> S[收集结果<br/>Tool Results]
    S --> T[生成最终响应]
    
    Q --> U[推送响应]
    T --> U
    
    U --> V{目标渠道}
    V -->|原渠道回复 | W[Channel Plugin<br/>Send Message]
    V -->|Canvas 渲染 | X[A2UI Live Canvas]
    V -->|语音输出 | Y[TTS Engine]
    V -->|Client App| Z[macOS/iOS/Android]
    
    W --> AA[外部用户收到回复]
    X --> AB[实时画布更新]
    Y --> AC[语音播放]
    Z --> AD[App 通知]
    
    style A fill:#bbf,stroke:#333,stroke-width:2px
    style AA fill:#bfb,stroke:#333,stroke-width:2px
    style AB fill:#bfb,stroke:#333,stroke-width:2px
    style AC fill:#bfb,stroke:#333,stroke-width:2px
    style AD fill:#bfb,stroke:#333,stroke-width:2px
```

---

## Agent Session 处理流程

```mermaid
flowchart TD
    A[收到用户消息] --> B[创建 Session<br/>Session ID]
    B --> C[加载配置<br/>Config Loading]
    C --> D[初始化 Context<br/>Transcript History]
    
    D --> E[构建 System Prompt]
    E --> F[添加身份标识<br/>Assistant Identity]
    F --> G[添加工具定义<br/>Tool Catalog]
    G --> H[添加技能列表<br/>Skills Registry]
    
    H --> I[附加用户消息<br/>User Message]
    I --> J[上下文压缩检查<br/>Token Count Check]
    
    J --> K{超出阈值？}
    K -->|是 | L[Compaction Engine<br/>压缩历史]
    K -->|否 | M[发送到 LLM]
    
    L --> M
    
    M --> N[Model Selection<br/>Auth Profile]
    N --> O[API 调用<br/>Provider Plugin]
    O --> P{响应类型}
    
    P -->|文本 | Q[流式输出<br/>Streaming]
    P -->|工具调用 | R[Tool Call<br/>Function Calling]
    P -->|错误 | S[错误处理<br/>Error Handler]
    
    R --> T{需要审批？}
    T -->|是 | U[等待用户批准<br/>Approval Request]
    T -->|否 | V[执行工具]
    
    U --> W{用户批准？}
    W -->|是 | V
    W -->|否 | X[跳过工具调用]
    
    V --> Y[收集结果<br/>Tool Results]
    Y --> Z[追加到 Transcript]
    Z --> AA[继续对话或返回]
    
    Q --> AB[推送给用户<br/>Channel/Canvas]
    X --> AB
    AA --> AB
    
    S --> AC{可恢复？}
    AC -->|是 | AD[故障转移<br/>Failover Model]
    AC -->|否 | AE[返回错误消息]
    
    AD --> M
    AE --> AB
    
    style A fill:#ff9,stroke:#333,stroke-width:2px
    style AB fill:#9f9,stroke:#333,stroke-width:2px
```

---

## 插件系统架构

```mermaid
flowchart TD
    A[Gateway 启动] --> B[Plugin Runtime 初始化]
    B --> C[扫描 extensions 目录]
    C --> D[加载 Plugin Metadata<br/>package.json]
    
    D --> E{Plugin 类型}
    E -->|Channel| F[注册 Channel Adapter<br/>WhatsApp/Telegram...]
    E -->|Provider| G[注册 Model Provider<br/>OpenAI/Anthropic...]
    E -->|Feature| H[注册功能模块<br/>Browser/Memory...]
    E -->|CLI Backend| I[注册命令后端]
    
    F --> J[Hook System 注册]
    G --> J
    H --> J
    I --> J
    
    J --> K[执行 Hook: gateway_start]
    K --> L[Channel Polling 启动]
    L --> M[Webhook 监听]
    
    M --> N{收到事件}
    N -->|消息 | O[Message Event Hook]
    N -->|状态变更 | P[Status Update Hook]
    N -->|认证过期 | Q[Auth Refresh Hook]
    
    O --> R[调用 Plugin API<br/>ctx.send/ctx.reply]
    P --> S[更新 Gateway State]
    Q --> T[刷新 Token]
    
    R --> U[写入 Transcript]
    S --> U
    T --> U
    
    U --> V[持久化存储<br/>JSON Store]
    
    style A fill:#9cf,stroke:#333,stroke-width:2px
    style V fill:#9cf,stroke:#333,stroke-width:2px
```

---

## 认证与授权流程

```mermaid
flowchart TD
    A[新设备连接请求] --> B{连接类型}
    B -->|WebSocket| C[Handshake Auth]
    B -->|HTTP API| D[Bearer Token Auth]
    B -->|Webhook| E[Signature Verification]
    
    C --> F{配对模式？}
    F -->|是 | G[输入 Pairing Code]
    F -->|否 | H[Token 验证]
    
    G --> I{Code 有效？}
    I -->|是 | J[颁发 Device Token<br/>JWT]
    I -->|否 | K[拒绝连接]
    
    H --> L{Token 有效？}
    L -->|是 | M[加载权限范围<br/>Scopes]
    L -->|否 | N[Token 刷新/重新认证]
    
    J --> O[建立 WebSocket 连接]
    M --> P[授权请求<br/>Authorization Check]
    
    P --> Q{权限足够？}
    Q -->|是 | R[执行操作]
    Q -->|否 | S[返回 403 Forbidden]
    
    R --> T[速率限制检查<br/>Rate Limiter]
    T --> U{超出限制？}
    U -->|是 | V[延迟/拒绝请求]
    U -->|否 | W[完成请求]
    
    K --> X[记录审计日志<br/>Audit Log]
    N --> X
    S --> X
    V --> X
    
    W --> Y[更新最后活跃时间]
    Y --> End([流程结束])
    
    style A fill:#fc9,stroke:#333,stroke-width:2px
    style End fill:#9f9,stroke:#333,stroke-width:2px
```

---

## 子代理协作流程

```mermaid
flowchart TD
    A[主 Agent 运行] --> B{需要子代理？}
    B -->|是 | C[创建 Subagent Session]
    B -->|否 | Z[继续单 Agent 流程]
    
    C --> D[分配独立 Workspace]
    D --> E[继承父 Agent 配置]
    E --> F[应用深度限制<br/>Depth Limit]
    F --> G[设置超时<br/>Timeout]
    
    G --> H[启动 Subagent]
    H --> I[并行/串行执行]
    
    I --> J{执行模式}
    J -->|并行 Parallel| K[同时启动多个]
    J -->|串行 Sequential| L[依次执行]
    
    K --> M[等待所有完成]
    L --> N[按顺序等待]
    
    M --> O[收集结果]
    N --> O
    
    O --> P{结果合并}
    P -->|汇总 | Q[Summarize Results]
    P -->|选择最佳 | R[Select Best]
    P -->|投票 | S[Voting Strategy]
    
    Q --> T[返回给主 Agent]
    R --> T
    S --> T
    
    T --> U[主 Agent 继续处理]
    U --> V[生成最终响应]
    
    Z --> V
    
    style A fill:#c9f,stroke:#333,stroke-width:2px
    style V fill:#9f9,stroke:#333,stroke-width:2px
```

---

## 上下文引擎流程

```mermaid
flowchart TD
    A[Transcript 加载] --> B[计算 Token 数量]
    B --> C{超过阈值？}
    
    C -->|否 | D[保持原样]
    C -->|是 | E[触发 Compaction]
    
    E --> F{压缩策略}
    F -->|Auto-Summary| G[LLM 摘要]
    F -->|Drop Old| H[删除旧消息]
    F -->|Hybrid| I[混合模式]
    
    G --> J[生成摘要块]
    H --> K[保留最近 N 轮]
    I --> L[摘要 + 截断]
    
    J --> M[替换原始内容]
    K --> M
    L --> M
    
    M --> N[更新 Transcript]
    N --> O[持久化存储]
    O --> P[继续对话]
    
    style A fill:#9cf,stroke:#333,stroke-width:2px
    style P fill:#9f9,stroke:#333,stroke-width:2px
```

---

## 工具执行流程

```mermaid
flowchart TD
    A[LLM 返回 Tool Call] --> B[解析工具定义]
    B --> C{工具类型}
    
    C -->|Shell 命令 | D[Bash Tools]
    C -->|浏览器控制 | E[Browser Plugin]
    C -->|文件操作 | F[Filesystem Tools]
    C -->|MCP 调用 | G[MCP Client]
    C -->|自定义工具 | H[Skill Execution]
    
    D --> I{需要审批？}
    E --> I
    F --> I
    G --> I
    H --> I
    
    I -->|是 | J[发送审批请求]
    I -->|否 | K[直接执行]
    
    J --> L{用户审批}
    L -->|批准 | K
    L -->|拒绝 | M[跳过执行]
    
    K --> N[执行工具]
    N --> O{执行成功？}
    
    O -->|是 | P[收集输出]
    O -->|否 | Q[捕获错误]
    
    P --> R[格式化结果]
    Q --> R
    
    R --> S[返回给 LLM]
    M --> S
    
    style A fill:#fc9,stroke:#333,stroke-width:2px
    style S fill:#9f9,stroke:#333,stroke-width:2px
```

---

## 数据流总览

```mermaid
sequenceDiagram
    participant User as 用户
    participant Channel as 渠道 Plugin
    participant GW as Gateway
    participant Session as Agent Session
    participant LLM as AI 模型
    participant Tools as 工具系统
    participant Memory = 记忆系统
    
    User->>Channel: 发送消息
    Channel->>GW: WebSocket 事件
    GW->>Session: 路由到 Session
    Session->>Memory: 加载历史
    Memory-->>Session: Transcript
    Session->>LLM: 构建 Prompt
    LLM-->>Session: 流式响应
    
    alt 需要工具调用
        Session->>Tools: 执行工具
        Tools-->>Session: 返回结果
        Session->>LLM: 提交结果
        LLM-->>Session: 最终响应
    end
    
    Session->>GW: 推送响应
    GW->>Channel: 发送回渠道
    Channel->>User: 显示回复
    
    Note over GW,Memory: 所有状态持久化
```

---

## 关键设计原则

### 1. 中心化网关 + 分布式节点
- **Gateway**: 控制平面，管理所有连接和状态
- **Clients**: 作为 WebSocket 客户端连接
- **Extensions**: 模块化插件系统

### 2. 会话隔离
- 每个对话在独立的 Agent Session 中运行
- 支持 Docker 沙箱隔离
- 独立的配置和上下文

### 3. 插件驱动架构
- 所有渠道、模型、功能都是插件
- Hook 系统允许拦截和扩展
- 热插拔能力

### 4. 安全优先
- 配对码验证机制
- Token -based 认证
- 速率限制和审计日志
- 敏感操作需要审批

### 5. 容错设计
- 模型故障自动转移
- 多 Auth Profile 轮换
- 会话状态持久化
- 自动重启和恢复

---

## 相关文档

- [项目介绍](../README.md)
- [愿景文档](../VISION.md)
- [贡献指南](../CONTRIBUTING.md)
- [代码注释指南](../代码注释指南.md)
- [插件原理](../插件原理.md)
- [Agent 原理](../Agent 原理.md)
