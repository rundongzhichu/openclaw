# OpenClaw 项目结构指引

本文档提供 OpenClaw 项目的完整结构说明,帮助开发者快速理解代码组织方式和核心文件功能。

## 📁 项目概览

OpenClaw 是一个运行在您自己设备上的个人 AI 助手,采用多通道架构,支持多种消息平台集成。项目使用 TypeScript 开发,基于 Node.js 运行时,采用 pnpm 作为包管理器。

### 技术栈

- **语言**: TypeScript (严格模式)
- **运行时**: Node.js >= 22.14.0 (推荐 Node 24)
- **包管理器**: pnpm@10.33.0
- **构建工具**: tsdown, Bun
- **测试框架**: Vitest
- **代码质量**: oxlint, oxfmt
- **通信协议**: WebSocket + RPC

---

## 🗂️ 目录结构详解

### 根目录核心文件

```
openclaw/
├── openclaw.mjs              # CLI 入口文件 - 命令行工具的启动脚本
├── package.json              # 项目配置 - 依赖、脚本、元数据
├── pnpm-workspace.yaml       # pnpm 工作区配置 - 管理 monorepo 结构
├── tsconfig.json             # TypeScript 编译配置
├── Dockerfile                # Docker 镜像构建配置
├── docker-compose.yml        # Docker Compose 编排配置
├── Makefile                  # Make 命令快捷方式
└── .npmrc                    # npm/pnpm 配置 - 扁平化依赖布局
```

#### 关键配置文件说明

**`openclaw.mjs`**
- CLI 二进制入口点
- 负责加载和启动 OpenClaw 命令系统
- 处理命令行参数解析和路由

**`package.json`**
- 定义项目元信息(名称、版本、描述)
- 声明所有运行时依赖和开发依赖
- 包含所有 npm scripts(构建、测试、运行等命令)
- 配置引擎要求(Node.js >= 22.14.0)
- 定义 pnpm 覆盖规则和补丁依赖

**`tsconfig.json`**
- TypeScript 编译器选项
- 启用严格类型检查
- 配置模块解析路径
- 定义输出目录和源映射设置

**`Dockerfile`**
- 多阶段构建配置
- 优化镜像大小和构建速度
- 包含生产环境运行时配置

---

### 📦 核心源码目录 (`src/`)

这是 OpenClaw 的核心业务逻辑所在,包含网关服务器、协议实现、插件系统等。

```
src/
├── gateway/                  # 网关服务器 - 核心控制平面
│   ├── server.ts            # WebSocket 服务器实现
│   ├── routes/              # API 路由定义
│   └── middleware/          # 请求中间件
│
├── channels/                 # 通道抽象层 - 消息渠道接口定义
│   ├── channel.ts           # 通道基类和接口
│   ├── router.ts            # 消息路由器
│   └── session.ts           # 会话管理
│
├── agents/                   # Agent 运行时 - AI 代理执行引擎
│   ├── runtime.ts           # Agent 运行时核心
│   ├── rpc.ts               # RPC 通信协议
│   └── tools/               # 内置工具集
│
├── plugins/                  # 插件系统 - 扩展机制核心
│   ├── sdk/                 # 插件开发 SDK
│   ├── loader.ts            # 插件加载器
│   ├── contracts/           # 插件契约定义
│   └── runtime/             # 插件运行时
│
├── config/                   # 配置管理
│   ├── schema.ts            # 配置 Schema 定义
│   ├── loader.ts            # 配置文件加载器
│   └── sessions/            # 会话配置
│
├── cli/                      # 命令行界面
│   ├── commands/            # CLI 命令实现
│   ├── onboard.ts           # 初始化向导
│   └── tui.ts               # 终端用户界面
│
├── protocol/                 # 通信协议
│   ├── websocket.ts         # WebSocket 协议实现
│   └── rpc.ts               # RPC 协议定义
│
├── security/                 # 安全模块
│   ├── pairing.ts           # DM 配对策略
│   ├── sandbox.ts           # 沙箱隔离
│   └── auth.ts              # 认证授权
│
├── media/                    # 媒体处理管道
│   ├── pipeline.ts          # 媒体处理流程
│   ├── transcription.ts     # 语音转文字
│   └── images.ts            # 图像处理
│
├── memory/                   # 记忆系统
│   ├── store.ts             # 记忆存储
│   └── retrieval.ts         # 记忆检索
│
├── tasks/                    # 任务调度
│   ├── scheduler.ts         # 任务调度器
│   └── cron.ts              # 定时任务
│
├── chat/                     # 聊天核心逻辑
│   ├── message.ts           # 消息处理
│   └── context.ts           # 上下文管理
│
├── web/                      # Web UI 服务
│   ├── static/              # 静态资源
│   └── api.ts               # Web API
│
├── infra/                    # 基础设施层
│   ├── net/                 # 网络工具
│   ├── tls/                 # TLS/SSL 处理
│   └── outbound/            # 出站连接管理
│
├── types/                    # 类型定义
│   ├── common.ts            # 通用类型
│   └── events.ts            # 事件类型
│
├── test-utils/               # 测试工具集
├── bootstrap/                # 引导程序
└── compat/                   # 兼容性层
```

#### 核心模块详细说明

**1. Gateway (网关)**
- **作用**: 整个系统的控制中心,处理所有客户端连接
- **关键文件**: `gateway/server.ts`
- **功能**: 
  - WebSocket 服务器监听(默认端口 18789)
  - 消息路由到对应通道和 Agent
  - 事件广播和订阅
  - 会话状态管理

**2. Channels (通道)**
- **作用**: 统一的消息渠道抽象层
- **关键文件**: `channels/channel.ts`, `channels/router.ts`
- **功能**:
  - 定义标准通道接口
  - 消息格式标准化
  - 会话隔离和激活模式
  - 支持 20+ 消息平台(WhatsApp, Telegram, Slack 等)

**3. Agents (代理运行时)**
- **作用**: AI Agent 的执行环境
- **关键文件**: `agents/runtime.ts`, `agents/rpc.ts`
- **功能**:
  - Pi Agent Runtime 集成
  - RPC 模式运行
  - 工具流式输出
  - 代码块流式渲染

**4. Plugins (插件系统)**
- **作用**: 可扩展的插件架构
- **关键文件**: `plugins/loader.ts`, `plugins/sdk/`
- **功能**:
  - 动态插件加载
  - 插件生命周期管理
  - 能力声明和发现
  - 热重载支持

**5. Config (配置管理)**
- **作用**: 统一的配置管理系统
- **关键文件**: `config/schema.ts`, `config/loader.ts`
- **功能**:
  - JSON/YAML 配置解析
  - 环境变量注入
  - 配置验证和默认值
  - 凭证安全管理

**6. Security (安全模块)**
- **作用**: 访问控制和隔离
- **关键文件**: `security/pairing.ts`, `security/sandbox.ts`
- **功能**:
  - DM 配对策略(未知发送者需要配对)
  - Docker 沙箱隔离非主会话
  - API 密钥加密存储
  - 网络访问控制

---

### 🔌 扩展目录 (`extensions/`)

包含所有通道集成、AI 提供商、工具等扩展模块。每个扩展都是一个独立的包。

```
extensions/
├── shared/                   # 共享工具库 - 扩展间共用代码
│
├── 通道集成 (Channels)/
│   ├── whatsapp/            # WhatsApp 集成 (Baileys)
│   ├── telegram/            # Telegram 集成 (grammY)
│   ├── discord/             # Discord 集成 (discord.js)
│   ├── slack/               # Slack 集成
│   ├── signal/              # Signal 集成
│   ├── matrix/              # Matrix 集成
│   ├── imessage/            # iMessage 集成
│   ├── wechat/              # 微信集成
│   ├── feishu/              # 飞书集成
│   ├── qqbot/               # QQ Bot 集成
│   └── ... (共 20+ 通道)
│
├── AI 提供商 (Providers)/
│   ├── openai/              # OpenAI API
│   ├── anthropic/           # Anthropic Claude
│   ├── google/              # Google Gemini
│   ├── ollama/              # Ollama 本地模型
│   ├── qwen/                # 通义千问
│   ├── moonshot/            # Moonshot Kimi
│   ├── deepseek/            # DeepSeek
│   ├── mistral/             # Mistral AI
│   ├── groq/                # Groq 快速推理
│   ├── together/            # Together AI
│   ├── openrouter/          # OpenRouter 聚合
│   └── ... (共 30+ 提供商)
│
├── 工具扩展 (Tools)/
│   ├── browser/             # 浏览器自动化 (Playwright)
│   ├── voice-call/          # 语音通话
│   ├── document-extract/    # 文档提取
│   ├── web-readability/     # 网页可读性
│   ├── image-generation-core/  # 图像生成核心
│   ├── video-generation-core/  # 视频生成核心
│   ├── music-generation/    # 音乐生成
│   └── ... (共 15+ 工具)
│
├── 记忆扩展 (Memory)/
│   ├── memory-core/         # 记忆核心接口
│   ├── memory-lancedb/      # LanceDB 向量存储
│   └── memory-wiki/         # Wiki 记忆
│
├── 诊断扩展 (Diagnostics)/
│   ├── diagnostics-prometheus/  # Prometheus 监控
│   └── diagnostics-otel/        # OpenTelemetry
│
└── 其他扩展/
    ├── acpx/                # ACPX 协议
    ├── skill-workshop/      # Skills 工作坊
    ├── tts-local-cli/       # 本地 TTS
    └── ... (共 50+ 扩展)
```

#### 扩展开发规范

每个扩展遵循以下结构:

```
extensions/<extension-name>/
├── src/
│   ├── index.ts            # 入口文件 - 导出插件定义
│   ├── plugin.ts           # 插件实现
│   └── ...                 # 其他源码
├── package.json            # 扩展包配置
├── README.md               # 扩展文档
└── tsconfig.json           # TypeScript 配置
```

**扩展类型标识**:
- **Channel**: 消息渠道集成
- **Provider**: AI 模型提供商
- **Tool**: 工具和能力扩展
- **Memory**: 记忆存储后端
- **Diagnostic**: 监控和诊断

---

### 📱 应用目录 (`apps/`)

包含桌面和移动 companion 应用。

```
apps/
├── macos/                   # macOS 菜单栏应用
│   ├── Sources/            # Swift 源代码
│   ├── Resources/          # 资源文件
│   └── Package.swift       # Swift 包配置
│
├── ios/                     # iOS 应用
│   ├── Sources/            # Swift 源代码
│   └── Package.swift       # Swift 包配置
│
├── android/                 # Android 应用
│   ├── app/                # 应用模块
│   ├── build.gradle.kts    # Gradle 构建配置
│   └── settings.gradle.kts # Gradle 设置
│
└── shared/                  # 共享代码
    └── kit/                # 跨平台工具包
```

#### Companion 应用功能

**macOS 应用**:
- 菜单栏快捷访问
- 屏幕录制权限管理
- 通知中心集成
- 语音唤醒检测

**iOS/Android 应用**:
- 相机拍照和视频录制
- 屏幕截图
- 位置信息获取
- 推送通知接收

---

### 🎨 Web UI (`ui/`)

基于 Lit 框架构建的 Web 控制面板。

```
ui/
├── src/
│   ├── components/         # Web Components
│   │   ├── chat/          # 聊天界面组件
│   │   ├── settings/      # 设置面板
│   │   ├── channels/      # 通道管理
│   │   └── agents/        # Agent 配置
│   ├── pages/             # 页面组件
│   ├── services/          # API 服务
│   ├── styles/            # 样式文件
│   └── index.ts           # 应用入口
├── public/                 # 公共资源
├── index.html             # HTML 模板
└── package.json           # UI 包配置
```

**UI 特性**:
- 实时聊天界面
- 通道配置管理
- Agent 会话监控
- 系统状态仪表盘
- 响应式设计

---

### 🧪 测试目录 (`test/`)

包含单元测试、集成测试和端到端测试。

```
test/
├── unit/                   # 单元测试
│   ├── channels/          # 通道测试
│   ├── agents/            # Agent 测试
│   └── plugins/           # 插件测试
├── integration/            # 集成测试
│   ├── gateway/           # 网关集成
│   └── websocket/         # WebSocket 测试
├── e2e/                    # 端到端测试
│   ├── scenarios/         # 测试场景
│   └── fixtures/          # 测试固件
├── contract/               # 契约测试
└── helpers/                # 测试辅助函数
```

**测试工具**:
- Vitest: 测试运行器
- JSCPD: 代码重复检测
- 自定义断言和模拟工具

---

### 📚 文档目录 (`docs/`)

项目文档站点,使用 Markdown 编写。

```
docs/
├── learning/               # 学习资料 (本目录)
│   ├── 项目结构指引.md    # 本文档
│   ├── 插件原理.md        # 插件系统详解
│   ├── 任务编排机制详解.md # 任务调度机制
│   ├── 代码理解.md        # 代码架构分析
│   └── ...
├── concepts/               # 概念指南
├── install/                # 安装指南
├── start/                  # 快速开始
├── channels/               # 通道文档
├── providers/              # 提供商文档
├── tools/                  # 工具文档
├── cli/                    # CLI 参考
├── gateway/                # 网关配置
├── plugins/                # 插件开发
├── reference/              # API 参考
└── assets/                 # 文档资源
```

---

### 🛠️ 脚本目录 (`scripts/`)

包含构建、部署、测试等自动化脚本(259 个脚本文件)。

```
scripts/
├── build/                  # 构建脚本
├── deploy/                 # 部署脚本
├── test/                   # 测试脚本
├── dev/                    # 开发工具
├── release/                # 发布脚本
├── ui.js                   # UI 构建和管理
├── run-node.mjs            # Node.js 运行器
└── ... (其他实用脚本)
```

**常用脚本**:
- `scripts/ui.js`: Web UI 构建和开发服务器
- `scripts/run-node.mjs`: 带 TypeScript 支持的 Node.js 运行器
- `scripts/profile-tsgo.mjs`: TypeScript 性能分析

---

### 🎯 Skills 目录 (`skills/`)

预打包的 Agent Skills,提供特定领域的能力。

```
skills/
├── <skill-name>/           # 各个 Skill 包
│   ├── SKILL.md           # Skill 描述
│   ├── references/        # 参考资料
│   ├── scripts/           # 执行脚本
│   └── assets/            # 资源文件
└── ... (共 53 个 Skills)
```

**Skill 示例**:
- 数据分析
- 代码审查
- 文档生成
- 研究助手

---

### 📦 包目录 (`packages/`)

独立的可发布包。

```
packages/
├── plugin-sdk/             # 插件开发 SDK
│   ├── src/               # SDK 源码
│   ├── package.json       # 包配置
│   └── README.md          # 使用文档
├── memory-host-sdk/        # 记忆主机 SDK
└── plugin-package-contract/# 插件包契约定义
```

---

### ⚙️ 配置和元数据文件

```
├── .github/                # GitHub 配置
│   ├── workflows/         # CI/CD 工作流
│   ├── ISSUE_TEMPLATE/    # Issue 模板
│   └── PULL_REQUEST_TEMPLATE/ # PR 模板
│
├── .vscode/                # VSCode 配置
│   ├── settings.json      # 工作区设置
│   └── extensions.json    # 推荐扩展
│
├── .gitignore             # Git 忽略规则
├── .dockerignore          # Docker 忽略规则
├── .npmignore             # npm 忽略规则
├── .prettierrc            # Prettier 格式化配置
├── .oxlintrc.json         # Oxlint 代码检查配置
├── .oxfmtrc.jsonc         # Oxfmt 格式化配置
├── vitest.config.ts       # Vitest 测试配置
├── tsdown.config.ts       # Tsdown 构建配置
├── knip.config.ts         # Knip 未使用代码检测
└── zizmor.yml             # Zizmor 安全检查
```

---

## 🔑 核心文件功能详解

### 1. 入口文件

**`openclaw.mjs`**
```javascript
// CLI 二进制入口
// 功能:
// - 解析命令行参数
// - 加载配置
// - 路由到对应命令
// - 启动网关或执行操作
```

### 2. 网关服务器

**`src/gateway/server.ts`**
```typescript
// WebSocket 服务器核心
// 功能:
// - 监听 ws://127.0.0.1:18789
// - 管理客户端连接
// - 路由消息到通道
// - 广播事件
// - 会话状态管理
```

### 3. 通道路由器

**`src/channels/router.ts`**
```typescript
// 消息路由器
// 功能:
// - 根据通道 ID 路由消息
// - 会话隔离和激活
// - 队列模式管理
// - 组聊和个人聊天区分
```

### 4. Agent 运行时

**`src/agents/runtime.ts`**
```typescript
// Pi Agent Runtime 集成
// 功能:
// - RPC 模式通信
// - 工具流式调用
// - 代码块流式渲染
// - 上下文管理
// - 错误处理和恢复
```

### 5. 插件加载器

**`src/plugins/loader.ts`**
```typescript
// 动态插件加载器
// 功能:
// - 扫描 extensions 目录
// - 加载插件包
// - 验证插件契约
// - 注册插件能力
// - 生命周期管理
```

### 6. 配置加载器

**`src/config/loader.ts`**
```typescript
// 配置管理系统
// 功能:
// - 读取 ~/.openclaw/openclaw.json
// - 合并默认配置和用户配置
// - 环境变量注入
// - 配置验证
// - 凭证解密
```

### 7. 安全配对

**`src/security/pairing.ts`**
```typescript
// DM 配对策略
// 功能:
// - 未知发送者需要配对
// - 配对码生成和验证
// - 白名单管理
// - 信任关系建立
```

---

## 🔄 数据流和架构

### 消息处理流程

```
外部消息 (WhatsApp/Telegram/etc.)
    ↓
通道适配器 (extensions/*/)
    ↓
WebSocket 网关 (src/gateway/)
    ↓
消息路由器 (src/channels/router.ts)
    ↓
Agent 运行时 (src/agents/runtime.ts)
    ↓
LLM 提供商 (extensions/providers/*)
    ↓
工具调用 (src/agents/tools/)
    ↓
响应生成
    ↓
反向路由回原通道
    ↓
发送给用户
```

### 插件加载流程

```
启动时扫描 extensions/
    ↓
读取每个扩展的 package.json
    ↓
验证插件契约 (plugin-package-contract)
    ↓
加载插件入口 (index.ts)
    ↓
注册插件能力 (capabilities)
    ↓
初始化插件实例
    ↓
就绪可用
```

### 配置加载优先级

```
1. 内置默认值 (最低优先级)
2. 配置文件 (~/.openclaw/openclaw.json)
3. 环境变量 (OPENCLAW_*)
4. 命令行参数 (最高优先级)
```

---

## 📖 开发工作流

### 1. 环境准备

```bash
# 安装依赖
pnpm install

# 初始化配置
pnpm openclaw onboard --install-daemon

# 启动开发服务器
pnpm gateway:watch
```

### 2. 构建流程

```bash
# 编译 TypeScript
pnpm build

# 构建 Web UI
pnpm ui:build

# 构建 Docker 镜像
docker build .
```

### 3. 测试流程

```bash
# 运行所有测试
pnpm test

# 运行单元测试
pnpm test:unit

# 运行集成测试
pnpm test:integration

# 代码重复检测
pnpm jscpd
```

### 4. 代码质量

```bash
# 代码检查
pnpm lint

# 代码格式化
pnpm format

# 类型检查
pnpm tsgo:all
```

---

## 🎓 学习路径建议

### 初学者路径

1. **阅读基础文档**
   - `docs/start/` - 快速开始
   - `docs/concepts/` - 核心概念

2. **理解架构**
   - `docs/learning/插件原理.md` - 插件系统
   - `docs/learning/任务编排机制详解.md` - 任务调度
   - `docs/learning/代码理解.md` - 代码架构

3. **实践操作**
   - 配置一个通道(如 Telegram)
   - 创建一个简单的 Skill
   - 调试网关日志

### 进阶路径

1. **深入源码**
   - `src/gateway/` - 网关实现
   - `src/channels/` - 通道抽象
   - `src/agents/` - Agent 运行时

2. **开发扩展**
   - 阅读 `packages/plugin-sdk/`
   - 参考现有扩展代码
   - 实现新的通道或提供商

3. **贡献代码**
   - 查看 `CONTRIBUTING.md`
   - 提交 Issue 或 PR
   - 参与社区讨论

---

## 🔍 常见问题定位

### 网关无法启动

检查位置:
- `src/gateway/server.ts` - 端口占用
- `~/.openclaw/openclaw.json` - 配置错误
- 日志: `~/.openclaw/logs/`

### 通道连接失败

检查位置:
- `extensions/<channel>/` - 通道实现
- 凭证配置: `~/.openclaw/credentials/`
- 网络代理设置

### 插件加载失败

检查位置:
- `src/plugins/loader.ts` - 加载逻辑
- 插件目录: `extensions/`
- 插件契约: `packages/plugin-package-contract/`

---

## 📊 项目统计

- **总文件数**: ~5000+ 文件
- **代码行数**: ~500,000+ 行
- **扩展数量**: 128+ 扩展
- **通道支持**: 20+ 消息平台
- **AI 提供商**: 30+ LLM 提供商
- **Skills**: 53+ 预置技能
- **测试覆盖率**: ~80% (核心模块)

---

## 🤝 贡献指南

详见 `CONTRIBUTING.md` 和 `AGENTS.md`

### 代码规范

- 使用 TypeScript 严格模式
- 遵循 oxlint 规则
- 编写单元测试
- 更新文档

### 提交流程

1. Fork 仓库
2. 创建分支 (`feature/xxx` 或 `fix/xxx`)
3. 提交更改
4. 运行测试和 lint
5. 提交 Pull Request

---

## 📞 获取帮助

- **文档**: `docs/` 目录
- **Issue**: [GitHub Issues](https://github.com/openclaw/openclaw/issues)
- **社区**: Discord / Matrix
- **安全**: `SECURITY.md`

---

## 📝 更新日志

本文档最后更新: 2026-04-28

如需更新此文档,请确保:
1. 反映了最新的目录结构变化
2. 更新了核心文件说明
3. 补充了新的扩展类型
4. 修正了过时的信息

---

**祝开发愉快! 🚀**
